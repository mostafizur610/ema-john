import React from 'react';

const ReviewItem = ({ products }) => {
    const { name, price, quantity } = product;
    return (
        <div>
            <h2>This is Review Product</h2>
        </div>
    );
};

export default ReviewItem;